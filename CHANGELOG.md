# سجل الإصدارات — محلات أبو بشار للملابس والأحذية

## الإصدار 2.1 — 2026‑09‑08

### أُضيف
- 🌗 **تبديل المظهر فاتح/داكن** — زر في ركن الشاشة؛ اختيارك يُحفظ في متصفحك ويُطبَّق قبل أول رسم (بلا وميض)، ويحدَّث لون شريط المتصفح معه.
- ⭐ **المفضلة (الروابط الأكثر استخدامًا)** — نجمة على كل بطاقة؛ البطاقة المفضّلة تنتقل إلى قسم «⭐ روابطي الأكثر استخدامًا» أعلى الصفحة (القسم لا يظهر إلا عند وجود مفضلة)، وتُحفظ في متصفحك.
- 📤 **زر مشاركة لكل بطاقة** — يفتح مشاركة الهاتف الأصلية (واتساب، الرسائل،…) مع بديل «نسخ رابط البطاقة» في المتصفحات غير الداعمة.
- 🔗 **رابط مباشر إلى البطاقة** — افتح الصفحة مع `#instagram` مثلًا فينزل إلى البطاقة ويُبرزها بهالة ذهبية؛ زر مشاركة كل بطاقة يرسل هذا النوع من الروابط.
- 🔎 **بحث عربي أذكى** — تطبيع الحروف (الألف/الياء/التاء المربوطة/التشكيل) ومطابقة الأسماء الشائعة باختصاراتها: «انستا» لانستغرام، «fb» لفيسبوك، «سناب» لسناب شات… بالعربية والإنجليزية.

### تحسّن
- 🐞 إصلاح أيقونة النسخ في زر نسخ كل بطاقة (كانت غير موجودة في قاموس الأيقونات فتظهر الزر فارغًا).
- ♿ أزرار المفضلة والمشاركة والتبديل ذات `aria-label`/`aria-pressed` صحيحة، والقفز المباشر يحترم `prefers-reduced-motion` ومنح البطاقة تركيزًا عند الوصول إليها.
- 🎨 توحيد لوني على متغيرات (CSS variables) لانسجام الثيمين، ودعم أصغر للثيم الفاتح في النافذة والإشعارات والتذييل.

### لم يتغيّر
- روابط الحسابات الأربعة وكل الروابط كما هي تمامًا.
- رابط الصفحة الرسمي، رمز QR، صورة المعاينة، والأيقونات.

> ⚠️ ملاحظة: أُنتج هذا الإصدار ونُشر في يوم إصداره نفسه، ولم يمرّ بفترة اختبار مسبقة كالمعتاد.

## الإصدار 2.0 — 2026‑09‑07

### أُضيف
- 🔎 **بحث فوري في الروابط** مع عدّاد نتائج ورسالة «لا يوجد رابط مطابق» وزر مسح.
- 📋 **زر نسخ لكل منصة** داخل بطاقتها (ينسخ رابط الحساب وحده مع إشعار تأكيد).
- 💬 **زر «إرسال على واتساب»** يفتح واتساب بنص جاهز ورابط الصفحة.
- 🖨️ **طباعة بطاقة QR** بتنسيق طباعة خاص: يُطبع الرمز واسم المحل والرابط فقط، دون بقية الصفحة.
- 🗂️ **مجموعات الروابط** (`GROUPS`): «تابعنا على منصاتنا الرسمية» / «فروعنا ومواقعنا» / «روابط إضافية» — والمجموعة بلا روابط لا تظهر.
- 🏷️ شارات المعرّفات في الترويسة: `@bshyr75625` و `bshyttbt26`.
- 📝 أسطر جاهزة (داخل تعليق) لإضافة تيك توك / تيليجرام / يوتيوب / خرائط جوجل / موقع إلكتروني بسطر واحد.
- 📄 ملف `CHANGELOG.md` وتوثيق الحقول الجديدة في `README.md`.

### تحسّن
- ♿ وصول أفضل: `aria-label` لكل زر، منطقة `role="status"` لإعلان نتائج البحث، `:focus-visible` واضح، احترام `prefers-reduced-motion`.
- 🧩 البطاقات أصبحت `article` مع رابط ممتد (stretched link) وزر نسخ مستقل — أوضح للقارئات الشاشية وأسهل للنقر.
- 🛡️ فتح/إغلاق نافذة QR يعمل حتى في المتصفحات التي لا تدعم `<dialog>` (بديل آمن بدل الخطأ).
- 🎨 تنعيم الحركة والتباين، ودعم أفضل للشاشات الصغيرة (360px فأقل).

### لم يتغيّر
- روابط الحسابات الأربعة كما هي تمامًا: واتساب، فيسبوك، انستغرام، سناب شات.
- رابط الموقع، رمز QR، صورة المعاينة `og-image.png`، والأيقونات.

## الإصدار 1.0 — 2026‑09‑02

- أول إطلاق: صفحة روابط واحدة (أسود × ذهبي، عربية RTL) مع بطاقة واتساب مميّزة، أزرار مشاركة/نسخ/QR، بيانات منظمة `ClothingStore`، و Open Graph.
